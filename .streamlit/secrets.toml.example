# Optional Streamlit Cloud / local secrets.
# The OpenAI API key is NOT stored here — each user enters their own key in the app UI.
#
# Copy to `.streamlit/secrets.toml` (gitignored) if you want debug logging:
# DEBUG = true
